{
  "name": "cpsc349-project3",
  "version": "1.0.0",
  "scripts": {
    "dev": "browser-sync start --server --startPath /index.html --files '*.html,*.css,*.js'",
    "test": "standard && vnu --skip-non-html --Werror *.html",
    "tailwind": "tailwindcss -i ./src/input.css -o ./output.css --watch",
    "start": "npm-run-all --parallel dev tailwind"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "standard": "^17.0.0",
    "tailwindcss": "^3.2.4"
  }
}
